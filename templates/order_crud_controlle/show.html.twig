{% extends 'base.html.twig' %}

{% block title %}Order{% endblock %}

{% block body %}
    <h1>Order</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order.id }}</td>
            </tr>
            <tr>
                <th>CreateAt</th>
                <td>{{ order.createAt ? order.createAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>State</th>
                <td>{{ order.state }}</td>
            </tr>
            <tr>
                <th>Carrier_name</th>
                <td>{{ order.carrierName }}</td>
            </tr>
            <tr>
                <th>Carrier_price</th>
                <td>{{ order.carrierPrice }}</td>
            </tr>
            <tr>
                <th>Delivery</th>
                <td>{{ order.delivery }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_order_crud_controlle_index') }}">back to list</a>

    <a href="{{ path('app_order_crud_controlle_edit', {'id': order.id}) }}">edit</a>

    {{ include('order_crud_controlle/_delete_form.html.twig') }}
{% endblock %}
